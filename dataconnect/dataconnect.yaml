specVersion: "v1beta"
serviceId: "thesystem"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "thesystem-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
